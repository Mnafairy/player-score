// export default function Card({ name, count, setCount }) {
//   return (
//     <div className="stepper">
//       <p>{name}</p>
//       <button onClick={() => setCount(Number(count) - 1)}>-</button>
//       <p>{count}</p>
//       <button onClick={() => setCount(Number(count) + 1)}>+</button>
//     </div>
//   );
// }
